#include <iostream> 
#include <fstream>

int uploadData(const char fileName[], dataCollectionStruct dataSet[]);

int main(){
	
}
int uploadData(const char fileName[], dataCollectionStruct dataSet[]){
	int numStudies = 0;
	int counter= 0;
	ifstream inFile;
}